<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Tienda Prado{% endblock %}</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Open+Sans:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Open Sans', sans-serif;
            color: #333;
        }

        /* Barra superior MUSEO DEL PRADO */
        .top-bar {
            background-color: #f5f5f5;
            border-bottom: 1px solid #e0e0e0;
            font-size: 0.75rem;
            letter-spacing: 0.08em;
            color: #555;
        }
        .top-bar a {
            color: #555;
            text-decoration: none;
        }
        .top-bar a:hover {
            text-decoration: underline;
        }

        /* Logo central */
        .logo-area {
            text-align: center;
            padding: 18px 0 12px 0;
            border-bottom: 1px solid #e8e8e8;
        }
        .logo-area h1 {
            font-family: 'Playfair Display', serif;
            font-size: 2rem;
            letter-spacing: 0.2em;
            color: #222;
            margin: 0;
            font-weight: 400;
        }
        .logo-icons {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            display: flex;
            align-items: center;
            gap: 16px;
            font-size: 1.15rem;
        }
        .logo-icons a {
            color: #333;
            text-decoration: none;
            position: relative;
        }
        .logo-icons .badge-carrito {
            position: absolute;
            top: -8px;
            right: -10px;
            background: #c00;
            color: #fff;
            border-radius: 50%;
            font-size: 0.65rem;
            width: 18px;
            height: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .lang-switch {
            font-size: 0.8rem;
            color: #666;
        }
        .lang-switch a { color: #333; text-decoration: none; }
        .lang-switch a:hover { text-decoration: underline; }

        /* Menú de categorías principal */
        .main-nav {
            border-bottom: 1px solid #e0e0e0;
            background: #fff;
        }
        .main-nav ul {
            list-style: none;
            display: flex;
            justify-content: center;
            gap: 0;
            margin: 0;
            padding: 0;
            flex-wrap: wrap;
        }
        .main-nav li a {
            display: block;
            padding: 12px 14px;
            font-size: 0.72rem;
            letter-spacing: 0.06em;
            text-transform: uppercase;
            color: #333;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.2s;
        }
        .main-nav li a:hover,
        .main-nav li a.active {
            color: #8b6914;
        }

        /* Sidebar de subcategorías */
        .sidebar-title {
            font-size: 0.85rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            border-bottom: 2px solid #333;
            padding-bottom: 8px;
            margin-bottom: 12px;
        }
        .sidebar-links {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        .sidebar-links li {
            margin-bottom: 6px;
        }
        .sidebar-links li a {
            font-size: 0.82rem;
            color: #2a7ab5;
            text-decoration: none;
        }
        .sidebar-links li a:hover {
            text-decoration: underline;
        }

        /* Cards de producto */
        .product-card {
            border: none;
            transition: transform 0.2s, box-shadow 0.2s;
            height: 100%;
            background: #fff;
        }
        .product-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 .4rem .8rem rgba(0,0,0,.1) !important;
        }
        .product-img {
            object-fit: contain;
            height: 260px;
            width: 100%;
            padding: 10px;
        }
        .product-card .card-body {
            text-align: center;
        }
        .product-card .card-title {
            font-size: 0.88rem;
            font-weight: 400;
            color: #333;
            line-height: 1.3;
        }
        .product-card .product-price {
            font-size: 0.95rem;
            color: #555;
            margin-top: 6px;
        }

        /* Filtros superiores */
        .filter-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            flex-wrap: wrap;
            gap: 10px;
        }
        .filter-bar .view-toggle a {
            color: #999;
            font-size: 1.1rem;
            margin-right: 6px;
            text-decoration: none;
        }
        .filter-bar .view-toggle a.active { color: #333; }
        .filter-bar select {
            font-size: 0.8rem;
            border: 1px solid #ccc;
            padding: 4px 8px;
        }
        .showing-info {
            font-size: 0.8rem;
            color: #777;
        }

        /* Vista lista */
        .list-item-img {
            width: 120px;
            height: 120px;
            object-fit: contain;
        }
        .list-item:hover {
            background: #fafafa;
        }

        /* Paginación estilo Tienda Prado */
        .pagination-prado {
            list-style: none;
            display: flex;
            gap: 6px;
            padding: 0;
            margin: 0;
        }
        .pagination-prado li a {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            border: 1px solid #ccc;
            color: #555;
            text-decoration: none;
            font-size: 0.9rem;
            transition: border-color 0.2s;
        }
        .pagination-prado li a:hover {
            border-color: #333;
        }
        .pagination-prado li.active a {
            border: 2px solid #333;
            font-weight: 700;
            color: #222;
        }

        main {
            min-height: calc(100vh - 250px);
        }

        footer {
            border-top: 1px solid #e0e0e0;
        }
    </style>
</head>
<body class="bg-white">

    <!-- 1. Barra superior: MUSEO DEL PRADO -->
    <div class="top-bar">
        <div class="container d-flex justify-content-between align-items-center py-1">
            <span>MUSEO DEL PRADO</span>
            <a href="#">IMAGE BANK</a>
        </div>
    </div>

    <!-- 2. Logo central + iconos -->
    <div class="logo-area">
        <div class="container position-relative">
            <a href="/" style="text-decoration:none;">
                <h1>TIENDA PRADO</h1>
            </a>
            <div class="logo-icons">
                <!-- Buscador (abre un campo) -->
                <a href="#searchCollapse" data-bs-toggle="collapse" aria-label="Buscar">🔍</a>
                <!-- Usuario: login o logout dinámico -->
                {% if usuario %}
                    <a href="/logout" aria-label="Cerrar sesión" title="Cerrar sesión ({{ usuario }})">🔓</a>
                {% else %}
                    <a href="/login" aria-label="Iniciar sesión" title="Iniciar sesión">👤</a>
                {% endif %}
                <a href="/carrito" aria-label="Carrito">
                    🛒
                    {% if total_carrito %}
                    <span class="badge-carrito">{{ total_carrito }}</span>
                    {% endif %}
                </a>
                <span class="lang-switch"><a href="#">ES</a> | <a href="#">EN</a></span>
            </div>
        </div>
    </div>

    <!-- Buscador colapsable -->
    <div class="collapse" id="searchCollapse">
        <div class="container py-2">
            <form action="/buscar" method="GET" class="d-flex">
                <input class="form-control me-2" type="search" name="busqueda" placeholder="Buscar productos..." aria-label="Buscar" {% if busqueda %}value="{{ busqueda }}"{% endif %}>
                <button class="btn btn-dark btn-sm" type="submit">Buscar</button>
            </form>
        </div>
    </div>

    <!-- 3. Menú de categorías principal -->
    <nav class="main-nav">
        <div class="container">
            <ul>
                <li><a href="#">Libros</a></li>
                <li><a href="#">Exposiciones</a></li>
                <li><a href="#">Colaboraciones</a></li>
                <li><a href="/" class="active">Impresiones</a></li>
                <li><a href="#">Hogar</a></li>
                <li><a href="#">Papelería</a></li>
                <li><a href="#">Textil y complementos</a></li>
                <li><a href="#">Niños</a></li>
                <li><a href="#">Regalos y accesorios</a></li>
                <li><a href="#">Colecciones</a></li>
                <li><a href="#">Outlet</a></li>
            </ul>
        </div>
    </nav>

    <!-- Contenido Principal -->
    <main class="container my-4">
        {% if ErrorMensaje %}
            <div class="alert alert-danger" role="alert">
                {{ ErrorMensaje }}
            </div>
        {% endif %}

        {% block content %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="bg-white text-center py-4 mt-auto">
        <div class="container">
            <p class="mb-0 text-muted" style="font-size:0.8rem;">© 2026 Tienda Prado SBW</p>
        </div>
    </footer>

    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
